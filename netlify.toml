[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# SPA fallback so deep links resolve to index.html (after function routes).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
